---
- name: Provision RabbitMQ golden image
  hosts: all
  become: yes
  vars:
    app_user: "rabbitmq"
    app_name: "rabbitmq"
    app_version: "{{ lookup('env', 'SERVICE_VERSION') }}"
    nexus_url: "{{ lookup('env', 'NEXUS_URL') }}"
    service_port: "{{ lookup('env', 'SERVICE_PORT') | default('8001') }}"
    eureka_port: "{{ lookup('env', 'EUREKA_PORT') | default('8761') }}"
    # ✅ ADD Redis variables
    redis_host: "localhost"
    redis_port: "6379"

  tasks:
    - name: Install Java 17
      apt:
        name: openjdk-17-jre-headless
        state: present
        update_cache: yes

    - name: Install RabbitMQ Server
      apt:
        name: rabbitmq-server
        state: present

    - name: Start and enable RabbitMQ
      systemd:
        name: rabbitmq-server
        state: started
        enabled: yes

    - name: Enable RabbitMQ management plugin
      command: rabbitmq-plugins enable rabbitmq_management
      register: result
      changed_when: "'enabled' in result.stdout"

    - name: Ensure rabbitmq user exists
      user:
        name: "{{ app_user }}"
        system: yes
        home: "/opt/{{ app_name }}"
        create_home: yes
        state: present
      ignore_errors: yes

    - name: Create application directory
      file:
        path: "/opt/{{ app_name }}"
        state: directory
        owner: "{{ app_user }}"
        group: "{{ app_user }}"
        mode: "0755"

    - name: Create application.yml
      copy:
        dest: "/opt/{{ app_name }}/application.yml"
        content: |
          server:
            port: ${SERVER_PORT:8001}
          
          spring:
            application:
              name: ${SPRING_APP_NAME:rabbitmq}
            redis:
              host: ${REDIS_HOST:localhost}
              port: ${REDIS_PORT:6379}
          
          # ✅ ADD THIS SECTION
          redis:
            api:
              url: ${REDIS_API_URL:http://localhost:1222}
          
          eureka:
            client:
              service-url:
                defaultZone: ${EUREKA_URL}
            instance:
              prefer-ip-address: true
              instance-id: ${spring.cloud.client.ip-address}:${server.port}
          
          management:
            endpoints:
              web:
                exposure:
                  include: health,info
            endpoint:
              health:
                show-details: always

    - name: Create environment file
      copy:
        dest: "/opt/{{ app_name }}/{{ app_name }}.env"
        content: |
          EUREKA_URL=http://localhost:{{ eureka_port }}/eureka/
          SERVER_PORT={{ service_port }}
          SPRING_APP_NAME={{ app_name }}
          # ✅ ADD THESE 3 LINES (WERE MISSING)
          REDIS_HOST={{ redis_host }}
          REDIS_PORT={{ redis_port }}
          REDIS_API_URL=http://{{ redis_host }}:1222
          REDIS_SERVICE_URL=http://{{ redis_host }}:1222/redis
          EUREKA_CLIENT_REGISTER_WITH_EUREKA=true
          EUREKA_CLIENT_FETCH_REGISTRY=true
          EUREKA_INSTANCE_PREFER_IP_ADDRESS=true

    - name: Download RabbitMQ JAR from Nexus
      get_url:
        url: "{{ nexus_url }}/com/ksapp/rabbitmq/{{ app_version }}/rabbitmq-{{ app_version }}.jar"
        dest: "/opt/{{ app_name }}/{{ app_name }}.jar"
        owner: "{{ app_user }}"
        group: "{{ app_user }}"
        mode: "0644"
        timeout: 120
      retries: 5
      delay: 10
      register: download_result
      until: download_result is success

    - name: Verify JAR file was downloaded
      stat:
        path: "/opt/{{ app_name }}/{{ app_name }}.jar"
      register: jar_file

    - name: Fail if JAR not downloaded
      fail:
        msg: "JAR file was not downloaded successfully from Nexus"
      when: not jar_file.stat.exists or jar_file.stat.size == 0

    - name: Create systemd service file
      copy:
        dest: "/etc/systemd/system/{{ app_name }}.service"
        content: |
          [Unit]
          Description=RabbitMQ API Service
          After=network.target rabbitmq-server.service
          
          [Service]
          User={{ app_user }}
          Group={{ app_user }}
          WorkingDirectory=/opt/{{ app_name }}
          EnvironmentFile=/opt/{{ app_name }}/{{ app_name }}.env
          ExecStart=/usr/bin/java \
            -Dspring.redis.host=${REDIS_HOST} \
            -Dspring.redis.port=${REDIS_PORT} \
            -Dredis.api.url=${REDIS_API_URL} \
            -Dserver.port=${SERVER_PORT} \
            -Dspring.application.name=${SPRING_APP_NAME} \
            -Deureka.client.service-url.defaultZone=${EUREKA_URL} \
            -jar /opt/{{ app_name }}/{{ app_name }}.jar
          Restart=always
          RestartSec=10
          SuccessExitStatus=143
          
          [Install]
          WantedBy=multi-user.target

    - name: Reload systemd
      systemd:
        daemon_reload: yes

    - name: Enable and start service
      systemd:
        name: "{{ app_name }}"
        enabled: yes
        state: started

    - name: Clean up logs before AMI creation
      shell: |
        journalctl --rotate
        journalctl --vacuum-time=1s
        rm -rf /var/log/journal/*
        truncate -s 0 /var/log/syslog 2>/dev/null || true
        truncate -s 0 /var/log/cloud-init.log 2>/dev/null || true
        truncate -s 0 /var/log/cloud-init-output.log 2>/dev/null || true
        rm -f /root/.bash_history
        rm -f /home/ubuntu/.bash_history
        rm -rf /tmp/*
        apt-get clean
      ignore_errors: yes

    - name: Display service status
      debug:
        msg: |
          ========================================
          RabbitMQ Deployment Complete!
          ========================================
          Service Name: {{ app_name }}
          Service Port: {{ service_port }}
          Eureka URL: Will be updated dynamically from SSM at boot
          Redis URL: Will be updated dynamically from SSM at boot
          ========================================